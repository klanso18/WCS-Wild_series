{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
    <h1>Comment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ comment.comment }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ comment.rate }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_comment_index') }}">Retour à la liste des commentaires</a>

    <a class="btn btn-primary" href="{{ path('app_comment_edit', {'id': comment.id}) }}">Modifier</a>

    {{ include('comment/_delete_form.html.twig') }}
{% endblock %}